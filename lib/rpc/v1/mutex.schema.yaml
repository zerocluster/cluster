title: Shared mutex
permissions: ["*"]

methods:
  try-down:
    title: Try down mutex
    persistentConnectionRequired: true
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string

  down:
    title: Down mutex
    persistentConnectionRequired: true
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string

  up:
    title: Up mutex
    persistentConnectionRequired: true
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string
