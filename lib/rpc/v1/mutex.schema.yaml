title: Shared mutex
permissions: ["*"]

methods:
  try-down:
    title: Try down mutex
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string

  down:
    title: Down mutex
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string

  up:
    title: Up mutex
    params:
      - name: id
        description: Mutex ID.
        schema:
          type: string
